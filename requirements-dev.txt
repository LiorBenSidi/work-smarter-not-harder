# Dev + CI tooling, pinned for reproducibility (course L6: pin your dependencies).
# Used by BOTH the GitHub-Actions CI and the local git hooks, so the dev tools are
# version-identical -- ruff/bandit/pytest behave the same locally and in CI.
# One-time local setup:  sh scripts/setup-hooks.sh   then   pip install -r requirements-dev.txt
ruff==0.15.17     # linter + the T20 no-print rule (config lives in pyproject.toml)
bandit==1.9.4     # security scanner (exact invocation lives in the hooks + ci.yml)
pytest==9.0.3     # test runner (local pre-push gate + CI). MUST equal tests/requirements.txt's pin —
                  # tests/Integration_Tests/test_pin_contract.py fails CI if the two drift apart.
