name: CI

# Automated checks that gate every PR before human review (course: Unit/Integration/System/Stress/Security + CI/CD).
# Make this a REQUIRED status check in main's branch protection so a PR can't be merged until it's green.
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  checks:
    name: checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install tooling + project deps (if present)
        run: |
          python -m pip install --upgrade pip
          pip install ruff bandit pytest
          # install project requirements as they appear (root + per-container)
          [ -f requirements.txt ] && pip install -r requirements.txt || true
          for d in web ai; do [ -f "$d/requirements.txt" ] && pip install -r "$d/requirements.txt" || true; done

      - name: Lint (ruff)
        run: ruff check .

      - name: Security scan (bandit)
        run: bandit -r . -q -x ./tests   # scan app code; exclude tests. Tune severity with -ll if it gets noisy.

      - name: Unit tests (pytest) — Unit/Integration/System/Security under tests/
        run: |
          set +e
          python -m pytest tests/ -q
          rc=$?
          # exit code 5 = "no tests collected" — tolerated only while scaffolding; remove this once tests exist.
          if [ "$rc" -ne 0 ] && [ "$rc" -ne 5 ]; then exit "$rc"; fi

  # Stress tests (locust) run on demand, not as a merge gate — they need a running stack.
  # Add a separate job (or a manual `workflow_dispatch`) once the app is up, per the course's stress-testing lab.
